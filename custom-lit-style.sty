\ProvidesPackage{custom-lit-style}

% --------------PACKAGES--------------
% ------------------------------------
% Standard packages that I use
% ------------------------------------
% --placing images
\usepackage{graphicx} 		% general image
\usepackage{subcaption}
\usepackage{caption} 	% subcaptions 
% --fancy tables with tabularx
\usepackage{tabularx} % requires defining some table lengths below
% --set up references, links, urls
\usepackage{hyperref}
% --maths
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
% --testing random text
\usepackage{blindtext}
% ------------------------------------
% some not so standard packages
% ------------------------------------
% --color
\usepackage{xcolor}
\usepackage{colortbl}
% --for reducing spaces between items
\usepackage{enumitem}
% --using mutiple columns and rows and rotations
\usepackage{multicol} % standardish
\usepackage{multirow} % same as abover
\usepackage{vwcol} % no so standard lengths
\usepackage{array} % for parbox?
% --page packages, not sure what I used these for
\usepackage{geometry} % setting page size
% --fancy headers
\usepackage{fancyhdr} % making a header
% -- added these for section formating in a better way
\usepackage{titlesec}
\usepackage{titletoc}
% -- only paragraph spacing (for readability)
\usepackage{setspace}
% -- adding booktabs tables
\usepackage{booktabs} % even fancier tables
% -- symbols, maybe standard
\usepackage{latexsym}

% --------------COMMANDS--------------
% ------------------------------------
% setting up colors
% ------------------------------------
% define UCD logo colors
\definecolor{ucdLogoDarkBlue}{RGB}{32,58,111}
\definecolor{ucdLogoDarkGold}{RGB}{244,186,23}
\definecolor{ucdLogoBlue}{RGB}{9,87,144}
\definecolor{ucdLogoGold}{RGB}{186,149,46}
% define other colors
\definecolor{customLightBlue}{RGB}{27,137,202}
\definecolor{customMintGreen}{RGB}{87,195,186}
\definecolor{customLightGreen}{RGB}{115,199,160}

% ------------------------------------
% hyperlink set up
% ------------------------------------
% how some of the links are set up
\hypersetup{colorlinks=false,linkcolor=text2,breaklinks=true}

% ------------------------------------
% Setting custom figures and table captions etc.
% ------------------------------------
% -- JP recommended
\renewcommand{\figurename}{\footnotesize\sffamily\bfseries FIGURE}
% -- Setting up captions
% -- for figures:
%		caption label is italic
%		caption text is bold / italic
%		justification is raggedright (i.e. left aligned)
\captionsetup[figure]{
	labelfont={sf,bf},
	textfont={sf},
	singlelinecheck=off,
	justification=raggedright}
% -- for subfigures:
%		caption label is bold
%		caption text normal.
% 		justification is centering
% 		singlelinecheck=off means that the justification setting is used even when the caption is only a single line long. 
% 		if singlelinecheck=on, then caption is always centered when the caption is only one line.
\captionsetup[subfigure]{
	labelfont={sf,bf},
	textfont=sf,
	singlelinecheck=off,
	justification=centering}
% -- here I am adding my table modifiers, supposedly the same as subfigure modifications
\captionsetup[table]{
	labelfont={sf,bf},
	textfont={sf},
	singlelinecheck=off,
	justification=centering}

% Here is a new way to make a figure with an environment called
% Figure. It was used to make text wrap around a figure
% keynote column figure. This new command to makes the figure 
% in a mini page. You do not have to use this
\newenvironment{Figure}
  {\par\medskip\noindent\minipage{\linewidth}}
  {\endminipage\par\medskip}

% ------------------------------------
% Setting up the section spacing
% ------------------------------------
% -- need to define spacing and size, all custom size names
%		ie. use \textSpace for the 1.1 pt size
\newcommand{\textSpace}{1.5} % used for spacing between lines
\newcommand{\sectionSpace}{-0.2}
\newcommand{\topSectionSpace}{-0.6}


% -- Formatting the titlesec stuff first 
%	took this from the titlesec documentation and commented 
%	stuff out, but some of the stuff is modified, but I dont 
%	remember exactly what
\titleformat{\chapter}[display]
{\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titleformat{\section}
{\normalfont\Large\bfseries\singlespacing}{\thesection}{1em}{}
\titleformat{\subsection}
{\normalfont\large\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}
{\normalfont\normalsize\bfseries}{\thesubsubsection}{1em}{}
\titleformat{\paragraph}[runin]
{\normalfont\normalsize\itshape}{\theparagraph}{1em}{---}[:~~~]
\titleformat{\subparagraph}[runin]
{\normalfont\normalsize\bfseries}{\thesubparagraph}{1em}{}
\titlespacing*{\chapter} {0pt}{5pt}{4pt}
% \titlespacing*{\section} {0pt}{3.5ex plus 1ex minus .2ex}{2.3ex plus .2ex}
% \titlespacing*{\subsection} {0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}
% \titlespacing*{\subsubsection}{0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}
% \titlespacing*{\paragraph} {0pt}{3.25ex plus 1ex minus .2ex}{1em}
% \titlespacing*{\subparagraph} {\parindent}{3.25ex plus 1ex minus .2ex}{1em}

% -- Now changing the spacing with the \sectionSpace and other
%	custom sizing defined at the beginning of the section
\titlespacing*{\section} {0pt}{-4.5ex}{-\sectionSpace ex}
\titlespacing*{\subsection} {0pt}{-\sectionSpace ex}{-\sectionSpace ex}
\titlespacing*{\subsubsection} {0pt}{-\sectionSpace ex}{-\sectionSpace ex}
\titlespacing*{\paragraph} {0pt}{-\sectionSpace ex}{-\sectionSpace ex}

% ------------------------------------
% Setting up the page styles now
% ------------------------------------

% -- Geometry package for margins
%	1 in margins
\geometry{letterpaper, top=1.0in, bottom=1.0in, left=1.0in, right=1.0in }
% 	.75 T/B margins, .5 L/R margins
%\geometry{letterpaper, top=0.75in, bottom=0.75in, left=0.5in, right=0.5in }

% -- fancyheader package finally 
\pagestyle{fancy}
\lhead{} %all empty
\chead{}
\rhead{}
\cfoot{\thepage} % adding the page at the center
\renewcommand{\footrulewidth}{0.5pt}

% -- And now for the `plain` pagestyle
\fancypagestyle{plain}

% -- paragraph spacing set up using the "setspace" package
%	other packages change all the spacing I believe, this does
%	only the paragraph
\setstretch{\textSpace}

% ------------------------------------
% And now the bibliography stuff
% ------------------------------------

% \usepackage{natbib}
\usepackage[square,sort,comma,numbers]{natbib}

% ------------------------------------
% Random stuff here
% ------------------------------------

% cool command for C++, try it ;)
% can't find the stack overflow source for this
% code snippet
\newcommand{\CC}{C\nolinebreak\hspace{-.05em}\raisebox{.4ex}{\tiny\bf +}\nolinebreak\hspace{-.10em}\raisebox{.4ex}{\tiny\bf +}}
\def\CC{{C\nolinebreak[4]\hspace{-.05em}\raisebox{.4ex}{\tiny\bf ++}}}